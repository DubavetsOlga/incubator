import styled from "styled-components"

const Skills = styled.section`

`;

const Skill = styled.div`
    width: 120px;
    height: 120px;
    gap: 120px;
    display: flex;
`;

export const S = {
    Skills,
    Skill,
}
